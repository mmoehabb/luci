{
  "name": "luci",
  "private": false,
  "version": "0.0.1",
  "main": "./dist/main.js",
  "bin": {
    "luci": "./dist/main.js"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "start": "bun run src/main.ts",
    "build": "bun build src/main.ts --outdir dist --minify --target bun",
    "compile": "bun build src/main.ts --compile --outfile ./bin/luci"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "chalk": "^5.6.2"
  }
}